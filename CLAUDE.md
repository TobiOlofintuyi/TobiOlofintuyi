# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this repository is

This is a GitHub profile README repository. Because the repository is named `TobiOlofintuyi/TobiOlofintuyi` (matching the account name), the `README.md` here is rendered on Tobi Olofintuyi's GitHub profile page.

There is no application code, build system, test suite, or linter — the only content is `README.md`.

## Working in this repository

- **README.md** is the profile page. Any edit to it changes what visitors see on the GitHub profile, so treat changes as user-facing content edits, not code changes.
- Preserve the existing formatting style: the README uses emoji section headers (👋🏾, 👀, 🌱, 💞️, 📫) followed by indented plain-text lines rather than standard Markdown headings and lists. Match that style when editing unless asked to restructure it.
- The HTML comment at the bottom of README.md is GitHub's boilerplate explaining the special-repository behavior; leave it in place.
- The profile describes Tobi as a product management leader, Co-Founder of Inyeon AI (mental health startup), and CEO of AJAO. Keep this content accurate to what the owner provides — don't invent or embellish biographical details.

## Common tasks

Since there is nothing to build or test, work here consists of Markdown/content edits:

- Verify rendering considerations manually (GitHub renders README.md with GitHub-flavored Markdown; the indented blocks render as code-style preformatted text, which is the intended look).
- Commit with clear messages describing the content change (existing history follows simple messages like "Update README.md").
